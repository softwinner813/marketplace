# Generated by https://jlopp.github.io/bitcoin-core-config-generator/

# [core]
# Run in the background as a daemon and accept commands.
daemon=1

# Wait for initialization to be finished before exiting. This implies -daemon.
#daemonwait=1

# Specify a non-default location to store blockchain and other data.
datadir=/disk1/Data/BitcoinFullNode/data

# [debug]
# Enable debug logging for all categories.
debug=net
# Location of the debug log
#debuglogfile=\\disk1\\Data\\BitcoinFullNode\\debug.log

# [rpc]
# Accept command line and JSON-RPC commands.
server=1

# Accept public REST requests.
rest=1

# Listen for JSON-RPC connections on this port
rpcport=8332

# Bind to given address to listen for JSON-RPC connections. This option is ignored unless -rpcallowip is also passed. Port is optional and overrides -rpcport. Use [host]:port notation for IPv6. This option can be specified multiple times. (default: 127.0.0.1 and ::1 i.e., localhost)
rpcbind=0.0.0.0

# Allow JSON-RPC connections from specified source. Valid for <ip> are a single IP (e.g. 1.2.3.4), a network/netmask (e.g. 1.2.3.4/255.255.255.0) or a network/CIDR (e.g. 1.2.3.4/24). This option can be specified multiple times.
rpcallowip=0.0.0.0/0

# Username for JSON-RPC connections
rpcuser=marketbitcoin

# Password for JSON-RPC connections
rpcpassword=123

# by setting this prune option, the daemon will not download the entire node
#prune=2000

# Setup transaction fee#
#paytxfee=0.001

# Setup Maximum transaction fee
#maxtxfee=0.002

# [Sections]
# Most options automatically apply to mainnet, testnet, and regtest networks.
# If you want to confine an option to just one network, you should add it in the relevant section.
# EXCEPTIONS: The options addnode, connect, port, bind, rpcport, rpcbind and wallet
# only apply to mainnet unless they appear in the appropriate section below.

# Options only for mainnet
[main]
rpcbind=0.0.0.0/0
rpcport=8332
# Options only for testnet
[test]

# Options only for regtest
[regtest]